package com.wsteam.wandscape.core.types;

/**
 * Attribute types that an NPC possesses.
 * <p>
 * Effective values are computed from base values + equipment modifiers.
 */
public enum AttributeType {
    RANGE,
    MANA_COST_MULTIPLIER,
    MAX_MANA,
    MANA_REGEN,
    MAX_HP,
    MOVE_SPEED
}
