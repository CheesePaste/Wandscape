# 交接：Wandscape 指南手册——待补的美术资源

面向美术。本文只列**要画什么、每张多大**，不规定配色与风格。

> **槽位权威表在 `docs/plan/patchouli-art-asset-spec.md` 第一节 1.3**（23 类槽位，逐条对着帕秋莉源码核过）。
> 本文不重复那张表——本文只回答「哪些还没画」。

---

## 一、当前是什么状态

手册的全部视觉都来自**一张 512×256 的图集**：

```
src/main/resources/assets/wandscape/textures/gui/guidebook/book.png
```

客户端已经没有绘制代码了，这张图就是书本身。

**它现在是程序生成的占位美术**：纯色填充、线性渐变、直线——**没有抗锯齿、没有纹理、没有噪点**。
所以核心工作量就是把它重画一遍。

---

## 二、必做

### 1. 重画书皮图集 `book.png`（512×256）

| 项 | 要求 |
|---|---|
| 尺寸 | **必须正好 512×256**（帕秋莉把尺寸硬编码在取样调用里，不对就整体错位） |
| 格式 | RGBA PNG，带 Alpha |
| 边距 | 不要留，按图集坐标摆放 |
| 槽位 | 23 类，见 spec 第一节 1.3 |
| 三个坑 | 见 spec 第一节 1.4，动笔前务必读 |

**参考成品**：`_refs/patchouli/Xplat/src/main/resources/assets/patchouli/textures/gui/book_brown.png`
——同一张图集布局、每个槽位都有正经美术，形状和位置照着它画。

### 2. 书物品贴图（16×16）

```
src/main/resources/assets/wandscape/textures/item/guide_book.png
```

现在已有（16×16，532 字节）。若要跟新书皮统一风格可以重画。

---

## 三、可选

| 资源 | 尺寸 | 现状 |
|---|---|---|
| 页面填充图 `filler_texture` | 128×128 | **未设**，用帕秋莉默认。想让空白页有自家质感就画 |
| 开合音效 `open_sound` / `flip_sound` | 2 个 ogg | **未设**，用帕秋莉默认 |

这两项要接的话需要同时在 `book.json` 里加字段，交回来我接。

---

## 四、不需要（已逐条确认）

| 曾以为要画 | 实际 |
|---|---|
| **正文插图**（256×256） | **不需要**——手册 217 页**全部**是 `patchouli:text` 纯文本页，没有任何 image 页或模板插图组件 |
| **条目 / 分类图标**（16×16） | **不需要**——全部直接引用物品 ID |
| **配方图集 `crafting_texture`**（128×256） | **不需要**——手册里没有合成页 |

---

## 五、当前观感与「原版帕秋莉」的已知差异

这几处**画不出来**（静态图集表达不了），说明如下，美术不必为它们白费功夫：

| 差异 | 原因 |
|---|---|
| 封面**没有外阴影** | 帕秋莉只贴 272×180 一块，画布内没有容纳外阴影的余地（原版书皮同样边缘零留白铺满） |
| 分类按钮**没有常驻金框** | 那些像素同时属于书页背景，画进去会在**每一页**的同一格出现；其悬停态也不走 hover 槽位 |
| 条目行**没有悬停金色流光** | 条目按钮不属于支持 hover 槽位的按钮类 |
| 书脊顶端**没有书签缎带** | 按需求移除，不必画 |

---

## 六、工程侧备注（开发者看）

- 现在 `book.png` 由 `gen_patchouli.py` 生成：`python gen_patchouli.py textures --force`
- 交付后二选一：
  - **方案 A（适合整张重画）**：美术的 PNG 直接替换，同时删掉 `gen_patchouli.py` 里的
    `build_book_texture()` 及全部绘制辅助函数与 `textures` 子命令，该图自此为手工资产
  - **方案 B（适合只补个别槽位）**：保留生成器，把美术出的那几格合成进去
- 验收：`./gradlew build`，游戏内打开手册逐页对照
- **注意**：`assets/wandscape/textures/item/` 下的贴图曾被一次分支合并覆盖过
  （`spell_scroll.png`、`guide_book.png` 都被另一条线替换过）。改这两个文件前后留意
  `git log --oneline -- <path>`，别再被合并无声覆盖
