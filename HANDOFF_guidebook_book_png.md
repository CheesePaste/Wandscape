# 交接：Wandscape 指南手册书皮图集 `book.png`

面向美术。目标是把游戏内《魔法小镇》指南手册（帕秋莉 Patchouli 手册）的外观整张重做。

---

## 一、这次改了什么（背景）

**改动前**：书的外壳、书脊、羊皮纸页、缎带和全部按钮，都是客户端每帧**用代码画**出来的
（`PatchouliBookRenderer.java`，830 行）。为此代码要反射进帕秋莉拿到书本的屏幕坐标，
占用了 1 个私有字段 + 3 个公开字段。

**改动后**：那份代码**已整体删除**，同样的画面被**烘进 `book.png` 这一张贴图**里。
现在玩家的手册长什么样，**完全由这张 PNG 决定**，代码只负责贴上去。

所以这张图不再是「占位图」，它就是书本身。美术在这里画什么，玩家就看到什么。

---

## 二、交付物

**一个文件，覆盖这个路径：**

```
src/main/resources/assets/wandscape/textures/gui/guidebook/book.png
```

| 项 | 要求 |
|---|---|
| 尺寸 | **必须正好 512 × 256**（游戏硬编码了这个尺寸，不对就会整体错位） |
| 格式 | PNG，**带 Alpha 通道**（RGBA） |
| 边距 | **不要留**，所有内容按下面的像素坐标摆放 |
| 导出 | 不要缩放、不要自动裁边 |

### 坐标约定

图集左上角 `(0,0)` 就是书本在屏幕上的左上角。帕秋莉把书本按 **272 × 180** 一块整贴上去，
所以 `(0,0)–(272,180)` 这一块就是**整本书的可见范围**。

---

## 三、槽位表

### 1. 书本本体

| 位置 | 尺寸 | 内容 |
|---|---|---|
| `(0,0)` | **272 × 180** | 整本书：封皮 + 书脊 + 左右双页 |

**正文位置是固定的，不能动**：帕秋莉把文字钉死在
`x 15..257`、`y 18..174` 这块区域里。羊皮纸页必须完整垫住它，并留出装订边。
左右两页的正文区分别是 `x 15..131` 和 `x 141..257`，中间 `x 132..140` 是书脊。

### 2. 按钮（每个都要**正常 + hover 两张**）

hover 图的位置 = 正常图位置 **向右偏移一个身位**（偏移量 = 该图的宽度），这是游戏写死的取样规则。

| 按钮 | 正常图位置 | hover 图位置 | 单张尺寸 |
|---|---|---|---|
| 下一页箭头 | `(272, 0)` | `(290, 0)` | 18 × 10 |
| 上一页箭头 | `(272, 10)` | `(290, 10)` | 18 × 10 |
| 书签页签（已有书签） | `(272, 160)` | `(285, 160)` | 13 × 10 |
| 书签页签（空位，加书签） | `(272, 170)` | `(285, 170)` | 13 × 10 |
| 返回上级 | `(308, 0)` | `(326, 0)` | 18 × 9 |
| **全部标为已读** | `(308, 31)` | `(319, 31)` | **11 × 11** |

补充说明：

- **「全部标为已读」的实际构成** = 底衬 `(285, 160)` 13×10 + 面上 `(308, 31)` 11×11，
  再叠一个绿色的 `+` 字符（这个字是代码画的，**不需要贴图**）。
- **`(285, 160)` 是共用槽**：它既是书签的 hover 图，又是「全部标为已读」的底衬。
  这是帕秋莉自己的设计，一张图要同时伺候两个用途。
- **`(272, 160)` 和 `(272, 170)` 在原版里长得一模一样**（「已有书签」和「加书签」不做区分），
  美术不必刻意画出区别。

### 3. 列表页与内嵌装饰（硬编码取样，位置要保住）

| 位置 | 尺寸 | 内容 |
|---|---|---|
| `(140, 180)` | 110 × 3 | 列表页的分隔条 |
| `(140, 183)` | 99 × 14 | 搜索框 |
| `(250, 180)` | 16 × 16 | 锁图标（未解锁的条目） |
| `(140, 197)` | 8 × 8 | 「未读」标记 |
| `(148, 197)` | 8 × 8 | 「待办」标记 |
| `(156, 197)` | 8 × 8 | 「已完成」标记 |
| `(405, 149)` | 106 × 106 | 图片 / 实体 / 多方块的**中空**外框（框内要透出内容） |
| `(0, 180)` | 140 × 31 | **着陆页横幅**（首页书名的衬底）。**目前是空的**，所以首页只有文字没有横幅 —— 想加就画这块 |

---

## 四、三条硬约束（画之前务必知道）

1. **羊皮纸页的位置不能动**。除了正文位置固定之外，还有一个隐藏依赖：
   分类页上的那些条目按钮，是**从书皮上原位置抠一块 20×20 当作按钮底色**的
   （按钮把自己的页面相对坐标直接当作取样坐标）。所以页面一旦移位或缩放，
   分类页的按钮底色会跟着错位。

2. **不要给书加外阴影**。游戏只贴 272×180 一块，画布内没有容纳外阴影的余地
   （原版帕秋莉书皮也是边缘零留白铺满的）。深度感只能靠封皮边缘自身的明暗。

3. **书内在跑的东西不能靠贴图遮**。书籍正文、物品图标、合成配方格子、翻页动效
   都是运行时画的，会盖在贴图之上。贴图上画得再细也会被盖住，别在正文区画花纹。

---

## 五、当前已知的缺口 / 粗糙点

这些是我知道的、需要美术处理的地方：

| 问题 | 说明 |
|---|---|
| 「全部标为已读」按钮不可见 | `(308,31)` / `(319,31)` 两格**完全是空的**，所以这个按钮现在是隐形的（但仍可点击） |
| 着陆页横幅缺失 | `(0,180,140,31)` 是空的，首页只有文字 |
| 按钮底板半透明 | 按钮底板目前用了 240/255 的不透明度，会透出背后的游戏画面。想要实心就画成全不透明 |
| 没有外阴影 | 见上文约束 2，是设计取舍不是遗漏 |
| **画面很粗糙** | 现在整张图是**程序生成的**：纯色填充、渐变、直线，**没有任何抗锯齿、纹理和噪点**。这就是需要美术重做的核心部分 |

如果游戏中看到的问题**不在这份清单里**，请截图并指出位置 —— 那样我才能判断是贴图规格的问题
还是代码的问题。

---

## 六、参考图

**帕秋莉自带书皮是同一张图集布局，每个槽位都有现成的美术可对照：**

```
_refs/patchouli/Xplat/src/main/resources/assets/patchouli/textures/gui/book_brown.png
```

它是 512×256、槽位与上表完全一致的成品，形状和位置照着它画即可。

---

## 七、交付之后的流程（给我或开发者看）

现在的 `book.png` 是 `gen_patchouli.py` **生成**的，跑 `python gen_patchouli.py textures --force` 会覆盖它。

美术交付后二选一：

- **方案 A（推荐，适合整张重画）**：美术的 PNG 直接取代 `book.png`，
  同时把 `gen_patchouli.py` 里的 `build_book_texture()` 及其绘制辅助函数、`textures` 子命令一并删掉，
  这张图从此变成手工维护的资产。
- **方案 B（适合只补缺口）**：保留生成器，只把美术画的那几格（比如「全部标为已读」）合成进去。

验收：`./gradlew build` 通过，游戏内打开手册逐页对照。
