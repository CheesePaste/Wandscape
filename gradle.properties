# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# ForgeGradle 6 要反编译 Minecraft，1G 不够；同时 FG6 不支持 Gradle 配置缓存，故不设 configuration-cache。
# ForgeGradle 6 decompiles Minecraft, so 1G is not enough; FG6 also has no Gradle configuration-cache support.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Environment Properties — 目标线：Minecraft 1.20.1 / Forge 47.x（降级副线）

# The Minecraft version must agree with the Forge version to get a valid artifact
minecraft_version=1.20.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.20.1,1.21)
# The Forge version must agree with the Minecraft version to get a valid artifact
forge_version=47.4.23
# The Forge version range can use any version of Forge as bounds or match the loader version range
forge_version_range=[47,)
# The loader version range can only use the major version of Forge/FML as bounds
loader_version_range=[47,)

# 映射通道与版本，经 org.parchmentmc.librarian.forgegradle 插件加载。
# Parchment 2023.09.03 是 1.20.1 线的最后一个发布版（2023-10 之后冻结）；版本串必须带 MC 后缀。
# Mapping channel/version, loaded via the org.parchmentmc.librarian.forgegradle plugin.
# 2023.09.03 is the last release for 1.20.1 (frozen after 2023-10); the version string carries the MC suffix.
mapping_channel=parchment
mapping_version=2023.09.03-1.20.1

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=wandscape
# The human-readable display name for the mod.
mod_name=Wandscape
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=MIT
# The mod version. See https://semver.org/
# 版本号规范见 docs/checklists.md：<版本>-<游戏版本>，游戏版本去点号（1.20.1 → -1201）。
mod_version=2.1.2-1201
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.wsteam.wandscape

# Iron's Spells 'n Spellbooks, 可选兼容依赖（其 irons_lib 为传递依赖，代码不直接引用）
irons_spells_version=1.20.1-3.16.3

# Curios API (Forge 1.20.1 线，被 Iron's/Goety 的强制传递依赖钉死在 5.x), 可选软依赖
curios_version=5.14.1+1.20.1

# Goety (CurseForge 项目 586095, 经 curse.maven 坐标；值 = 目标版本的 CurseForge 文件 ID), 可选软依赖
goety_version=8894243

# Patchouli (帕秋莉手册), 可选软依赖
patchouli_version=1.20.1-85-FORGE

# JEI (Forge 1.20.1 线), 可选运行时依赖，compileOnly 不打包
jei_version=15.59.0.212

# 注：车万女仆 (Touhou Little Maid) 在 1.20.1 侧不兼容（决策 2026-09-23）——
# 其数据附件无 Forge 对应物，整包不做，故此处不再声明该依赖。
# Note: Touhou Little Maid is out of scope for the 1.20.1 line (decision 2026-09-23).
