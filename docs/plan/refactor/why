为什么我认为需要重构？
1.多次定义
NPC属性有初始属性和升级提升属性，按理来说应该统一。但是NPC的招募那里按level算了一次属性，magehut又算了一次，导致如果只改了一个地方就会不统一，NPC属性修改极其困难。同时，在Tourist里面也定义了一波属性，在core/types/NPCAttributes里面又有一个0引用的记录类记录了NPC属性。一个NPC属性被定义了六七次，维护完全是灾难。
2.软件包混乱
wandscape下面有几十个软件包，做整合包和附属模组的直接被吓死。core/types/有一堆NPC属性的类，shared/data/又有一堆，对AI来说还能grep，对人类来说几乎无法找到自己想要的内容。engine/core/shared包到底有多少的硬性区别我也没看出来，纯粹的反人类的软件包分割。
3.代码量灾难
模组有10万行以上的代码，这还是有很多内容没有加入的情况下，这种代码量对独立开发者来说压力很大，加上上面两条，这里面70%以上可能都是无意义的防御性编程和各种包之间的连接。
4.JSON噩梦
JSON是为了兼容性和热重载，building，crafting_recipe用JSON也非常好，但是我们有十几个文件夹里面全是JSON，包括blueprints，这是不是太滥用了？
5.UI堆积
模组的UI设计没问题，但是当时建筑少，完全没有想到后面会堆积成这样。
每一个新的UI画面就要写一个新的类，关键是长宽等都不一样，每个建筑打开的UI也不一样。这导致如果我想在每个建筑UI中都加入一个拆除按钮或者修复按钮，就要修改每一个UI。
6.线路复杂缠绕
模组开发定了一条规则，不能直接引用别的包，但是这导致一系列严重问题，首先就是原来一层就能解决的事情，现在要在各个类里面绕来绕去，shared包堆积了一堆不相干内容，其中很多根本不是给附属模组和整合包作者留下的，而是纯粹为了搭桥而写的类，这种类毫无营养价值，这也导致附属模组和整合包作者，以及我们后续开发维护，必须要从这个庞大的管线网络中找到自己要接进去的点，这实在是太难了。
况且不能直接引用别的包这条规则也有了不少反例，名存实亡。
7.lang灾难
模组在开发到一半时都没加入i18n，之后突然加入导致了一系列问题，首先是整个lang文件夹已经变成了一坨，足足有一千多行，其次是大量的switch-case，严重影响了可读性，搭配上第六条绕来绕去的内容，导致很可能你加入一个简单的功能，就要修改十几个二十几个文件，在模组代码中绕一圈，这真是太折磨了。
8.命名灾难
模组足足有二百多个文件，这其中由于我们的法师生物一会叫法师一会叫NPC，导致模组内Mage和NPC两种命名共存。更要命的是，模组的命名毫无规律可言，我记得好像明明有engine软件包，但是在软件包外面却还有叫Engine的类。
9.装腔作势的文档与过分复杂的流程
模组有architecture和docs两个文档系统，architecture已经过时了，而docs里面装了大量无意义的decisions，并且其中很多文档也不见得好到哪里。或许这个docs写法非常专业，但是我们是业余的模组开发者，不是专业的软件公司，这些文档不会有专人去审批，写完了估计也没人看。
同时模组的提交流程也很灾难，因为我们业余开发，所以很多时候都是同时开多个终端跑AI，但是我们有写文档，编译java，跑build，跑test，写test，核对，检查修改文件，提交这一套复杂流程，这套流程我估计能占消耗token的1/10，完全是烧钱机器。同时，很多时候两个AI同时修改一个文件，然后都要来问我怎么提交，有些时候还会撤销掉别人的提交，或者提交到一半发现把别人提交带进去了所以撤回，有时候还要开stash然后别的AI就发现自己的更改怎么不见了。这套流程又费钱又得不偿失，必须修改。
10.最灾难的Log系统
最后我必须点名批评Log系统。我们模组自始至终都没有给log加上限制，导致各种log满天飞，其中很多都没什么用完全要删。为了解决这个问题我们加了自定义的log类，但是这没多少用，一方面是开启自定义log后没用的log满天飞，不开就一点log没有，fliter难用的很，非常折磨。
更要命的是很多时候不止Log，还会打印debug消息到聊天区，屏幕上，这不止是开发问题，更影响玩家游戏体验了。