# 数值基线快照（balance baseline）

> 快照日期 **2026-09-26**（§1.3 除外，见该节顶部警示） | Minecraft NeoForge 1.21.1 | 分支 1.21.1
> 本文是 `balance/`（未入库）那套分析工具的**数值结论快照**，入库是为了不跑工具也能查。

- **【何时读】**：调平衡、评估「某条产出线够不够」、写文档/手册要引用具体数字、或怀疑某处数值被人动过时——先查这里，对不上再跑工具。
- **【不包含什么】**：推导过程、工具用法与口径细节（那些在 `balance/README.md`，随工具走、不入库）。
- **【会过期】**：§1–§3 全是快照数字，任何一次平衡调整都会让它们漂移。改完数值**必须**回来更新，或者至少改掉顶部的日期。§4 是代码常量，只有改 `Config.java` / 常量文件时才会变。
- **【怎么刷新】**：见 §5。

---

## 一、殖民地等级与经验

### 1.1 公式（代码事实）

| 项 | 值 | 出处 |
|---|---|---|
| 升级所需经验 | `expToNext(L) = 440·(L+1) + 20·(L+1)²` | `ColonyLevelManager.expToNext` |
| 游客等级 == 小镇 | **700** exp | `Config.colony.expEqualLevel` |
| 游客等级 > 小镇 | **1400** exp | `Config.colony.expAboveLevel` |
| 游客等级 < 小镇 | **350** exp（同级的一半，不再归零） | `ColonyLevelManager.computeExpContribution` |
| 等级上限 | 30（达到后经验不再累积） | `Config.colony.maxLevel` |

经验只在游客**满条离场**时结算；不满条给 0。

### 1.2 每级所需经验

| 级 | 所需 | 累计 | 级 | 所需 | 累计 |
|---|---|---|---|---|---|
| 1 | 960 | 960 | 16 | 13,260 | 102,560 |
| 2 | 1,500 | 2,460 | 17 | 14,400 | 116,960 |
| 3 | 2,080 | 4,540 | 18 | 15,580 | 132,540 |
| 4 | 2,700 | 7,240 | 19 | 16,800 | 149,340 |
| 5 | 3,360 | 10,600 | 20 | 18,060 | 167,400 |
| 6 | 4,060 | 14,660 | 21 | 19,360 | 186,760 |
| 7 | 4,800 | 19,460 | 22 | 20,700 | 207,460 |
| 8 | 5,580 | 25,040 | 23 | 22,080 | 229,540 |
| 9 | 6,400 | 31,440 | 24 | 23,500 | 253,040 |
| 10 | 7,260 | 38,700 | 25 | 24,960 | 278,000 |
| 11 | 8,160 | 46,860 | 26 | 26,460 | 304,460 |
| 12 | 9,100 | 55,960 | 27 | 28,000 | 332,460 |
| 13 | 10,080 | 66,040 | 28 | 29,580 | 362,040 |
| 14 | 11,100 | 77,140 | 29 | 31,200 | 393,240 |
| 15 | 12,160 | 89,300 | — | 1→30 合计 | **393,240** |

单级需求从 960 涨到 31,200（32 倍）。换成人话：升 30 级约等于 **561 个满条同级游客**。

### 1.3 实际升级节奏（sim_spatial，8 种子）

> ⚠ **2026-09-26 起本节数字已过时**：商店 `profit_rate` 全表减半（§二），攒钱变慢，升级节奏必然拖长。
> 下表是减半**之前**的跑分，重跑 `sim_spatial.py --scenario prog / idle` 前别引用。

1 游戏日 = 20 分钟现实。

| 阶段 | prog（有钱就建下一档 + 动态扩建） | 目标（`balance/README.md`） |
|---|---|---|
| 1→5 | 4 日 / 1.3 h | ≤ 3 日 / 1 h，**略超** |
| 5→10 | 6 日 / 2.0 h | ≤ 15 日 / 5 h |
| 10→15 | 9 日 / 3.0 h | ≤ 30 日 / 10 h |
| 15→20 | 17 日 / 5.7 h | ≤ 60 日 / 20 h |
| 20→30 | 47 日 / 15.7 h | — |
| **1→30 全程** | **83 日 / 27.7 h** | ≤ 40 h |

关键节点：Lv5 = D4(1.3h)、Lv10 = D10(3.3h)、Lv15 = D19(6.3h)、Lv20 = D36(12.0h)、Lv30 = D83(27.7h)。

挂机线（`--scenario idle`，只建解锁 ≤1 的建筑 + 动态扩建）：**中位 Lv.11**（8 种子 10–11），到 Lv.11 要 191 天 ≈ 63.7 h。设计目标是「1 级档基线卡在 ≈9 级」，实测高了约 2 级。

---

## 二、游客线元素产出

口径来自 `calc_balance.py` 的 A5 段（解析上限口径：商店补满不计成本、游客全住得下、停留消费按 2× 钱包估、**不含服务建筑的 elementOutput**）。所以这是**上限**，真实值更低。

> 2026-09-26：商店的 `profit_rate` 全表减半（1.0→0.5、0.8→0.4、0.7→0.35、0.6→0.3、0.5→0.25），下表已按新值重跑。
> A5 只把**毛利**算作产出（`利润 = profit_rate × 单价 × 数量`），所以利润率的这一刀就是产出的精确一半；
> 而实际入账是 `cost × (1 + profit_rate)`（成本那半不动），真金白银只降 17%–25%。两个口径别混。

日利润（元素入账 / 游戏日）：

| 攒钱期 | 档位 | earth | wood | water | fire | metal | wind | dark | 合计 |
|---|---|---|---|---|---|---|---|---|---|
| Lv.4 | 1→5 | 1,658 | 5,706 | 552 | 1,633 | 1,720 | 1,953 | 279 | **13,501** |
| Lv.9 | 5→10 | 3,665 | 13,541 | 1,016 | 5,558 | 13,160 | 4,458 | 798 | **42,196** |
| Lv.14 | 10→15 | 5,874 | 38,603 | 3,333 | 8,848 | 20,950 | 7,097 | 3,063 | **87,768** |
| Lv.19 | 15→20 | 11,766 | 64,178 | 5,889 | 11,964 | 48,759 | 11,639 | 3,935 | **158,130** |

两个形状特征：

- **wood 一直是最大项**（占 32%–43%），metal 次之。每一档的**瓶颈元素**分别是 wood（1→5、5→10）、earth（10→15）、dark（15→20）——瓶颈不在产出最多的那一项，而在产出最少的项。
- 游客线随殖民地等级增长 **11.7 倍**（13,501 → 158,130），因为游客数量、钱包、商品档位都跟着等级涨。

攒钱天数（A5 口径，目标 = 下一档新增建筑造价 ÷ 日利润）：前三档 **5–6 天**，15→20 档要 **40 天**（瓶颈 dark）。所以**造价不是瓶颈**，三值推进才是。

---

## 三、探索线元素产出

口径：每个结构「一张战利品表 = 一箱」的算术平均；exp 与元素同源（`exp = 元素总值 ÷ exp_ratio`，`exp_ratio = 5.0`，无危险乘数）。模组**不自带声明区域**，原版结构与其他模组一样落在生成档（`mode: fixed`，值 = 估价器算出的期望），数字由 `balance/explore_reward.py` 复算，已对 22/22 张原版格式表与游戏落盘值逐字节对齐。本次删声明区域**不改任何数字**：生成档写下的值就是 `derived` 会现算的那个期望，`exp_ratio` / `variance` / `loot_share` 三项也都仍是 `DEFAULT`。

**元素一律按「玩家实际到手」算，不是估算向量。** 两者差别很大，别混：估价器产出的价值向量是**战利品形状**的（极度偏 metal），而 `ExplorationRewardRange.rollElements` 发放时会按 `loot_share`（默认 0.5）保留一半形状、把另一半总额平摊给七元素。于是到手金额近似 `value[e]/2 + 总额/14`——末地城的估算向量是 100% metal，到手只有 57%。`out/explore_reward.txt` 的 B/C 段列的是**估算向量**，F 段才是到手口径。

表里的元素数字**已乘 `Config.exploration.elementMultiplier`（当前 1.0，即估价原值）**，经验数字已乘 `expMultiplier`（当前 1.0）。2026-09-26 把 elementMultiplier 从 2.0 调回 1.0，下表已是调回后的数（上一版表里的元素数字除以 2 即可对上）。表里「一箱 = 天游客线」两列不变：游客线同期也减半（§二），分子分母同除 2。

### 3.1 原版地貌（结构汇总）

| 结构 | 表数 | 均/箱 元素 | 均/箱 exp | metal 占比 | 其余六元素各 | 一箱 = 天游客线（Lv.4 / Lv.19） | 按瓶颈元素（Lv.4 wood / Lv.19 dark） |
|---|---|---|---|---|---|---|---|
| 下界遗迹 | 5 | 46,259 | 9,252 | 44% | 3,334–9,183 | 3.43 / 0.29 | 0.58 / 0.85 |
| 末地城 | 1 | 14,083 | 2,816 | 57% | 1,006–1,019 | 1.04 / 0.09 | 0.18 / 0.26 |
| 远古之城 | 2 | 11,834 | 2,366 | 46% | 924–1,436 | 0.88 / 0.07 | 0.17 / 0.36 |
| 地牢 | 1 | 9,183 | 1,836 | 48% | 783–818 | 0.68 / 0.06 | 0.14 / 0.21 |
| 沙漠神殿 | 1 | 7,175 | 1,435 | 56% | 513–549 | 0.53 / 0.05 | 0.09 / 0.14 |
| 沉船与宝藏 | 4 | 6,858 | 1,372 | 25% | 494–2,570 | 0.51 / 0.04 | 0.10 / 0.13 |
| 废弃矿井 | 1 | 4,607 | 921 | 55% | 329–376 | 0.34 / 0.03 | 0.07 / 0.09 |
| 丛林神庙 | 1 | 3,177 | 635 | 56% | 227–253 | 0.24 / 0.02 | 0.04 / 0.06 |
| 要塞 | 3 | 784 | 157 | 44% | 58–130 | 0.06 / 0.00 | 0.02 / 0.02 |
| 村庄 | 16 | 666 | 133 | 44% | 51–108 | 0.05 / 0.00 | 0.02 / 0.01 |

「表数」是同一区域下被匹配到的战利品表张数（村庄 16 张、下界遗迹 5 张……），上面的均值是**每张表**的平均，不是一趟结构的合计。

### 3.2 结论：头部三个结构够用，腰部以下偏紧，且随等级迅速贬值

1. **头部结构很慷慨。** 下界遗迹平均一箱 ≈ 3.43 天整个殖民地前期的游客产出（其中两张宝库表各 ≈ 7.4 天），末地城 1.04 天、远古之城 0.88 天。**但这是前 3 名**——第 4 名起掉到 0.68 天以下，村庄/要塞只有 0.05–0.06 天。
2. **形状没有看上去那么歪。** 到手后 metal 占 25%–57%，其余六元素各分到约 7%。瓶颈元素（wood / earth / dark）每箱能拿 **50–3,350**，不是估算向量显示的 0。按瓶颈元素折算：头部结构 0.17–0.85 天，中段 0.04–0.14 天，村庄/要塞 0.01–0.02 天。
3. **越到后期越不值钱。** 游客线随等级涨 11.7 倍，箱子价值固定，所以同一箱从「3.43 天」缩到「0.29 天」。探索是**前期加速器**，不是中后期补给线——15 级之后最强的箱子也只值 0.29 天的产出。

一句话：**「元素产量低」的直觉在结构分布上成立**——十个结构里只有头部三个是真补给，腰部以下给的不到一天的零头；而在元素形状上，`loot_share` 的平摊已经把 metal 偏斜稀释掉一大半，瓶颈元素并不像估算向量看起来那么惨。2026-09-26 已把 `elementMultiplier` 从 2.0 调回 1.0（经验不动），上表是调回之后的数。

次要原因（占比小，别当主因）：原版战利品表里最常出现却**没有 `element_mappings`** 的物品是 `minecraft:enchanted_book`（约 4.6 次/箱，因为附魔类函数会把书换成附魔书，而映射表里没有附魔书），其次是 `suspicious_stew`、各种 `*_armor_trim_smithing_template`、`filled_map`。把它们按同类补上价，箱子价值只涨 **0.3%–4%**。

### 3.3 结构间的价差来自 metal 映射

`minecraft:netherite_ingot` 一锭 = **fire 16,384 + metal 33,792 = 50,176** 元素值（对照：`diamond` 1,024、`iron_ingot` 64、`emerald` 256）。堡垒宝藏表的**估算向量**（未过 loot_share、未乘倍率，即 B/C 段那个数）因此被顶到 10 万级，与村庄的 666 拉开 **150 倍**。要压探索的价差，动这里比动任何区域参数都直接——但它是全局建造经济的共用映射，改前先看 `calc_balance.py` 的 A1 造价表。

---

## 四、关键常量速查（代码事实）

| 项 | 值 | 出处 |
|---|---|---|
| 殖民地初始元素 | 每元素 2,000 | `Config.colony.initialElementCount` |
| 离线收益系数 | 0.2 | `Config.colony.offlineIncomeMultiplier` |
| 游客钱包 | `500 + 等级 × 200`；旅行基金 = 3× 钱包 | `Config.tourist.baseWallet` / `walletPerLevel` |
| 游客停留 | 2–4 游戏日 | `Config.tourist.stayMinDays` / `stayMaxDays` |
| 游客每日新增 | `uniform[5+(L−1), 7+(L−1)]` | `Config.tourist.baseSpawnCount` + `spawnRangeWidth` |
| 同时在场上限 | 150 | `Config.tourist.maxPerColony` |
| 游客等级分布 | `L−1` 40% / `L` 40% / `L+1` 20% | `TouristSpawnSystem.rollTouristLevel` |
| 游客三值需求 | `60 + 10×(等级−1)`，按画像权重分给 C/M/W | `Config.tourist.needBase` / `needPerLevel` |
| 游客精力上限 | 100 | `Config.tourist.maxEnergy` |
| 满条经验 | 700 / 1400 / 350 | 见 §1.1 |
| 探索 exp_ratio | **5.0** | `ExplorationRewardSpec.DEFAULT_EXP_RATIO` |
| 探索 loot_share | 0.5（发放时保留战利品形状的那一半，其余平摊七元素） | `ExplorationRewardSpec.DEFAULT_LOOT_SHARE` |
| 探索 variance | 0.25（上下限 ±25%） | `ExplorationRewardSpec.DEFAULT_VARIANCE` |
| 探索估算缩放 | ×2.0（`ESTIMATE_SCALE`） | `ExplorationLootEstimator` |
| 探索奖励倍率 | 经验 ×1.0、**元素 ×1.0**（结算最后一步；只乘元素不碰经验） | `Config.exploration.expMultiplier` / `elementMultiplier` |
| 商店元素产出倍率 | 1.0（全局阀门，乘在离线折减之后；基准值在建筑 JSON 的 `profit_rate`） | `Config.shop.elementMultiplier` |
| 服务设施元素产出倍率 | 1.0（同上；基准值在建筑 JSON 的 `element_output`） | `Config.service.elementMultiplier` |
| 商店入账 | `商品元素估价 × (1 + profit_rate)`；`profit_rate` 现取 0.25–0.5 | 建筑 JSON `shop.profit_rate` |
| 招募法师 | 每元素 10,000 | `Config.tavern.recruitCostPerElement` |
| 仓库物品上限 | 20,000 | `Config.warehouse.itemCapacity` |

---

## 五、怎么刷新这张表

`balance/` 不入库，工具都在那里。改完建筑 JSON 或数值后：

```bash
cd balance
python extract_buildings.py                 # 刷新建筑造价/三值
python calc_balance.py                      # 刷新 §2 游客线（A5 段）
python sim_spatial.py --scenario prog --days 500   # 刷新 §1.3 prog 曲线
python sim_spatial.py --scenario idle --days 200   # 刷新 §1.3 挂机线
python explore_reward.py                    # 刷新 §3 探索线 → out/explore_reward.txt
```

然后把上面对应的表**手工搬到本文**——这一步没有脚本，靠人。所以：

- 只改了「快照数字」相关的数值却没回来更新本文，本文就是错的，比没有更糟。
- §1.1、§4 是代码常量，改代码时顺手改，不依赖跑工具。

### 快照本身的已知偏差

- **生成档会与默认值静默分叉**：`<world>/wandscape/generated_regions/` 是「某次运行时」的快照，启动时只要匹配到就跳过重算。改过默认值（如 `exp_ratio`）后，老世界的生成文件仍按旧值结算；本文 §3 引用的是**当前代码默认值**，不是某个存档里的旧值。清掉该目录、下次开服即重写。
  原版结构现在也在这一档（模组不再自带声明区域），所以这条对 §3 的全部数字成立，不再只是「其他模组的表」的问题。
- **`irons_spellbooks` 的战利品表复算不准**（4/44）：它用了自家 entry/function 类型，`balance/explore_reward.py` 没复刻。本文 §3 只覆盖原版结构，不受影响。
- **`*/inject/chests/*` 载荷表不折 exp**：那是全局战利品改装的注入目标，估价器只读宿主表的 pools、不跑 loot modifier。共 50 张，已从统计中排除。
- **A5 不是 sim**：§2 是解析上限口径，真实体验以 `sim_spatial` 为准（后者含视野/移动/排队/精力/visited 约束）。两者对不上时按 sim 保守调整、用 A5 验证方向。
